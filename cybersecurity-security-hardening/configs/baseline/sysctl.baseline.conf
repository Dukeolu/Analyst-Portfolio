# /etc/sysctl.conf — AS-FOUND baseline on erp-integration-01.meridianmfg.local
# Distribution defaults, largely untouched.

net.ipv4.ip_forward = 0
# No explicit hardening parameters set below this line — accept_redirects,
# rp_filter, and related network-hardening sysctls are left at their
# distribution defaults, which are more permissive than this environment needs.
